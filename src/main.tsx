import React from 'react';
import ReactDOM from 'react-dom/client';
import { NewTabPage } from './pages/NewTabPage';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><NewTabPage /></React.StrictMode>);
